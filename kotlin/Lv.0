// 문자열 출력하기
fun main(args: Array<String>) {
    val s1 = readLine()!!
    println(s1)
}

// n보다 커질 때까지 더하기
class Solution {
    fun solution(numbers: IntArray, n: Int): Int {
        var answer = 0
        for (num in numbers) {
            if (answer > n) break
            answer += num
        }
        return answer
    }
}
